{
  "name": "streamly-api",
  "version": "1.0.0",
  "description": "A Mongoose API module for a streaming platform (channels + videos). Complete the TODOs.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.1"
  }
}
